// Determine se um número é ímpar ou par utilizando uma estrutura if e o operador %.
export function questao2(numero) {
  if (numero % 2 === 0) {
    console.log('O número é par');
  } else {
    console.log('O número é ímpar');
  }
}